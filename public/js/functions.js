function getID(id) {
  return document.getElementById(id);
}

function keyboardControl(e) {
  let keycode = event.key;

  switch (keycode) {

    case 'ArrowLeft':
      break;

    case 'ArrowRight':
      break;

    case 'Escape':
      break;

    default:
    return;
  }
}
